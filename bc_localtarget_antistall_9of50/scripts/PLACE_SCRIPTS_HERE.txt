Place required custom train / play / eval / source scripts here.

Expected structure after adding scripts:

  scripts/
  └── custom/
      ├── bc_detector_expert/
      │   ├── play_bc_rnn_target_conditioned.py   ← main play script
      │   └── train_bc_rnn_target_conditioned.py  ← main train script
      ├── run_5cube_camera_hierarchical_success_fixed.py
      ├── localtarget/                             ← LocalTarget helper scripts
      ├── antistall/                               ← anti-stall guard scripts
      └── hdf5_utils/                              ← HDF5 collection/conversion/audit scripts

Source directories on local machine:
  /home/a0loshi1/robotics/IsaacLab/scripts/custom/
  /home/a0loshi1/robotics/IsaacLab/source/
  /home/a0loshi1/robotics/task-franka-cubestack-base/scripts/
  /home/a0loshi1/robotics/task-franka-cubestack-base/source/

Reference: /tmp/bc_policy_needed_files_manifest.txt (on local machine)

Rules:
- Include ONLY files relevant to this BC policy
- Do NOT include unrelated RL, PPO, or other scripts
- Do NOT include __pycache__/ or *.pyc files
- Do NOT include unrelated old failed run scripts

After adding scripts, the play command is:

  cd /path/to/IsaacLab
  ./isaaclab.sh -p /path/to/test_stacking/bc_localtarget_antistall_9of50/scripts/custom/bc_detector_expert/play_bc_rnn_target_conditioned.py \
    --base_script /path/to/test_stacking/bc_localtarget_antistall_9of50/scripts/custom/run_5cube_camera_hierarchical_success_fixed.py \
    --checkpoint /path/to/test_stacking/bc_localtarget_antistall_9of50/policy/best.pt \
    --task Isaac-Stack-5Cube-Franka-IK-Rel-v0 \
    --num_envs 1 \
    --max_steps 700 \
    --enable_cameras \
    --disable_yaw_grasp \
    --device cuda:0

To add from your local machine:
  cp -r /home/a0loshi1/robotics/IsaacLab/scripts/custom/ bc_localtarget_antistall_9of50/scripts/
  git add bc_localtarget_antistall_9of50/scripts/
  git commit -m "Add BC policy scripts"
  git push
